# Eval case 01 — expected (baseline)

_Empty until the first verified run._ After the first launch produces a
grant-report.md you've checked and trust, paste it here. From then on, a new
agent version is only promoted if it still meets `../../outcome.md` against this
baseline (run `../run-evals.sh`).
