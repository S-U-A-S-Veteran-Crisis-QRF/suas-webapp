# Eval case 01 — input

This case's input is the SUAS profile + task in `../../first_prompt.txt` (the v0
launch input). Once the bracketed profile fields are filled and a run produces a
verified, good report, save that report as `expected.md` in this folder — it
becomes the regression baseline for future agent versions.
